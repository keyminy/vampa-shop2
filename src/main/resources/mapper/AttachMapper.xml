<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.vampa.mapper.AttachMapper">
	<select id="getAttachList" resultType="com.vampa.model.AttachImageVO">
		SELECT * FROM vam_image
		WHERE bookId=#{bookId}
	</select>
</mapper>